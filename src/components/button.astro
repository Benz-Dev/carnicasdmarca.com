---
import { twMerge } from "tailwind-merge";

const _class = Astro.props.class;
const {...props} = Astro.props;

---

<button class={twMerge(["px-6 py-1.5 bg-red-600 text-white rounded-full font-semibold text-base hover:brightness-110", _class])} {...props}>
    <slot/>
</button>